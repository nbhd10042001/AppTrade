@using Microsoft.AspNetCore.Authorization;
@inject IAuthorizationService authorizationService

@{
    var result = await authorizationService.AuthorizeAsync(this.User, "ViewManageMenu");
}
@if (result.Succeeded)
{
    <a class="site-a-white mx-1" asp-controller="AdminCP" asp-action="Index" asp-area="AdminControlPanel">
        <i class="fas fa-cog"></i> Quản trị</a>
}
